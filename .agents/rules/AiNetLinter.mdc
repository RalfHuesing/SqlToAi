---
description: C#-Codequalität — Automatisch generierte AiNetLinter-Richtlinien (alwaysApply)
globs: *.cs
alwaysApply: true
---
# C#-Codequalität (AiNetLinter)
Auto-generiert durch AiNetLinter 1.0.72 aus `C:\Daten\Entwicklung\Ralf\SqlToAi\tests\SqlToAi.Tests\AiNetLinter\rules\SqlToAi.rules.json`. Neuen Produktionscode direkt konform schreiben.

## Kurz-Stil
- `sealed` für konkrete Klassen.
- Kurze, flache Methoden (≤60 Zeilen); ab 5 Parametern ein Input-`record`.
- `#nullable enable` am Dateianfang.
- Kein leeres `catch` — Log + sichtbarer Fehler oder `throw;`.
- Kein `dynamic`; `out` nur in `Try*`-Methoden (private Methoden ausgenommen).
- Klassen-Kopplung (Footprint) klein halten: max. 2500 transitive Zeilen eigener Typen.

## Grenzwerte (Produktion)
| Regel | Limit | Praxis |
| :--- | :---: | :--- |
| `MaxLineCount` | **500** | Datei splitten wenn sie wächst. |
| `MaxMethodLineCount` | **60** | Eine Aufgabe pro Methode; Rest extrahieren. |
| `MaxMethodParameterCount` | **4** | Ab Überschreitung: `record` als Parameter-Object. |
| `MaxCyclomaticComplexity` | **12** | Weniger `if`/`switch`/`&&`/`||` pro Methode (McCabe). |
| `MaxCognitiveComplexity` | **15** | Weniger Verschachtelung; Early Return bevorzugen (kognitiv). |
| `MaxInheritanceDepth` | **3** | Komposition vor Vererbung. |
| `MaxMethodOverloads` | **5** | Methoden mit eindeutigen Namen bevorzugen. |
| `MaxConstructorDependencies` | **5** | Verantwortlichkeit aufteilen bei Überschreitung. |
| `AIContextFootprint` | **2500** | Kopplung reduzieren; eigene Typen-Abhängigkeiten minimieren. |
| `MaxDirectoryDepth` | **4** | Ordner nicht unnötig tief schachteln. |
| `MaxDirectoryChildren` | **0** | 0 = deaktiviert; zu viele Dateien/Unterordner → Unterverzeichnis anlegen. |
| `MaxBoolParameterCount` | **1** | 0 = deaktiviert; bool-Parameter in Parameter-Object bündeln. |
| `MaxPartialClassFiles` | **2** | 0 = deaktiviert; Logik in eigenständige Klassen auslagern (z. B. XyzChecker). |
| `MaxPublicMembersPerType` | **15** | 0 = deaktiviert; Typ aufteilen oder Member kapseln. |
| `MaxLinqChainLength` | **0** | 0 = deaktiviert; lange LINQ-Ketten in Teilschritte aufteilen. |
| `CSS_MaxCssLineCount` | **300** | CSS-Datei splitten oder in Scoped CSS ueberfuehren. |
| `CSS_MaxCssSelectorComplexity` | **3** | Maximal 3 Selektor-Segmente; Scoped CSS nutzen. |
| `JS_MaxJsLineCount` | **150** | JavaScript-Datei aufteilen oder Logik nach C# migrieren. |
| `RAZOR_MaxRazorLineCount` | **300** | Razor-Komponente aufteilen oder Teilbereiche extrahieren. |
| `RAZOR_MaxRazorCodeBlockLines` | **20** | @code-Block in Code-Behind-Datei (.razor.cs) verschieben. |
| `RAZOR_MaxMarkupNestingDepth` | **6** | Innere Bereiche in eigene Komponenten extrahieren. |
| `RAZOR_MaxControlFlowBlocks` | **8** | Teilbereiche in eigenstaendige Komponenten extrahieren. |
| `RAZOR_MaxForeachNestingDepth` | **2** | Innere Schleife in eigene Komponente extrahieren. |
| `RAZOR_MaxComponentParameterCount` | **10** | Verwandte Parameter in Parameter-Objekt zusammenfassen. |

## Compound Suppressions (kontextabhängige Limiten)
Folgende Regeln gelten mit relaxiertem Limit wenn alle Bedingungen erfüllt sind:

| Regel | Bedingung | Effektives Limit | Severity | Grund |
|:--|:--|:--|:--|:--|
| `MaxMethodLineCount` | CyclomaticComplexity ≤ 3 AND CognitiveComplexity ≤ 5 | **150** | `warning` | Initialisierungs- und Builder-Methoden sind semantisch flach. LOC bei CC≤3 ist nicht mit Fehleranfälligkeit korreliert (Palomba et al., 2018). Über 150 Zeilen: sichtbar als warning, kein Build-Fehler. |

## agent-resilience
- **EnforceNoSilentCatch** — `catch` immer mit Log + sichtbarem Fehler oder `throw;` — nie leer.
- **BanAsyncVoid** — 'async void' verboten; Ausnahme: Event-Handler mit '(object sender, EventArgs e)'.
- **BanBlockingTaskAccess** — '.Wait()'/'.Result'/'.GetAwaiter().GetResult()' verboten; verwende 'await'.

## architecture
- **EnforceNamespaceDirectoryMapping** — Namespace muss Verzeichnispfad entsprechen (Modus: `rules.json`).
- **DetectAndBanPhantomDependencies** — Keine unauflösbaren `using`; kein `Type.GetType`/`Activator.CreateInstance` für App-Typen.

## test-coverage
- **EnableTestSentinel** — Für komplexe Typen: Testklasse, `typeof(T)` oder `// @covers T`.

## general
- **EnforceSealedClasses** — `sealed` für konkrete Klassen; Ausnahmen: Suffixe in `rules.json → SealedClassExemptSuffixes`.
- **EnforcePascalCase** — Öffentliche Typen/Methoden/Properties: PascalCase.
- **EnforceAsciiIdentifiers** — Bezeichner duerfen keine Nicht-ASCII-Zeichen (Umlaute, Akzente, Emojis) enthalten.
- **EnforceSemanticNaming** — Keine generischen Namen in oeffentlichen Signaturen oder werkzeug-generierte Dummy-Namen (MyRegex, NewMethod, Class1) auf Deklarationen.
- **EnforceNullableEnable** — `#nullable enable` am Dateianfang jeder `.cs`-Datei.
- **AllowTryPatternOutParameters** — `out` in `Try*`-Methoden erlaubt.
- **AllowCancellationShutdownCatch** — `OperationCanceledException` beim Shutdown abfangen erlaubt.
- **EnforceValueObjectContracts** — Klassen mit `*ValueObject`-Suffix: `record` oder `readonly struct`.

Ausnahmelisten (Immutability, Sealed, Namespace-Segmente): `rules.json`.

## Deaktiviert
Linter erzwingt nicht (trotzdem anstreben): `EnforceXmlDocumentation`, `EnforceExplicitStateImmutability`, `EnforceMinimalApiAsParameters`, `EnforceResultPatternOverExceptions`, `PreventContextDependentOverloads`.
Trotzdem immer verboten: `dynamic`; `out` (außer in `Try*`).

## Projekt-Overrides (nur Abweichungen)

**`*.Tests`:** `MaxMethodLineCount` **100**; `EnforceSealedClasses` aus. Details: `rules.json`.

Details: `rules.json`, `AiNetLinter.exe --docs <name>`.
